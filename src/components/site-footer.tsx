export function SiteFooter() {
  return (
    <footer className="border-t border-border bg-card/80 py-3 text-center text-xs text-muted-foreground backdrop-blur">
      Apresenta.me {new Date().getFullYear()} — todos os direitos reservados
    </footer>
  );
}
